/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: "export",
  reactStrictMode: false,
  crossOrigin: "anonymous",
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
